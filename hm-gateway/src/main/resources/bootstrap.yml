spring:
  application:
    name: cart-service
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml
